{
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter 'better-skills' build",
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter 'better-skills' typecheck",
    "dev": "bun run --filter 'better-skills' dev",
    "build:binary": "bun run scripts/build-binary.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "tsdown": "^0.21.0-beta.2",
    "typescript": "^5.8.0"
  }
}
